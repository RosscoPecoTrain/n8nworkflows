{
  "name": "Timesheet Setup - Create Dated Folder & Files",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "timesheet-setup",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook_trigger",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300]
    },
    {
      "parameters": {
        "functionCode": "// Parse incoming timesheet date and format it\nconst timesheetDate = $input.first().json.timesheetDate;\n\nif (!timesheetDate) {\n  throw new Error('timesheetDate is required (format: M/D/YYYY or MM/DD/YYYY)');\n}\n\n// Parse date\nconst parts = timesheetDate.split('/');\nif (parts.length !== 3) {\n  throw new Error('Invalid date format. Use M/D/YYYY');\n}\n\nconst month = parseInt(parts[0]).toString().padStart(2, '0');\nconst day = parseInt(parts[1]).toString().padStart(2, '0');\nconst year = parts[2];\n\nconst formattedDate = `${year}.${month}.${day}`;\nconst folderName = formattedDate;\nconst basePath = '/home/node/.openclaw/workspace/projects/n8n/timesheet/output';\nconst outputPath = `${basePath}/${folderName}`;\nconst templatePath = '/home/node/.openclaw/workspace/projects/n8n/timesheet/n8n-workflows/templates';\n\nreturn {\n  json: {\n    timesheetDate,\n    formattedDate,\n    folderName,\n    outputPath,\n    templatePath,\n    basePath\n  }\n};"
      },
      "id": "format_date_node",
      "name": "Format Date",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [500, 300]
    },
    {
      "parameters": {
        "functionCode": "const fs = require('fs');\nconst path = require('path');\n\nconst outputPath = $input.first().json.outputPath;\n\ntry {\n  // Create directory if it doesn't exist\n  if (!fs.existsSync(outputPath)) {\n    fs.mkdirSync(outputPath, { recursive: true });\n  }\n  \n  return {\n    json: {\n      success: true,\n      folderCreated: true,\n      path: outputPath\n    }\n  };\n} catch (error) {\n  throw new Error(`Failed to create folder: ${error.message}`);\n}"
      },
      "id": "create_folder_node",
      "name": "Create Output Folder",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [750, 300]
    },
    {
      "parameters": {
        "functionCode": "const fs = require('fs');\nconst path = require('path');\n\nconst outputPath = $input.first().json.outputPath;\nconst templatePath = $input.first().json.templatePath;\nconst formattedDate = $input.first().json.formattedDate;\n\nconst wordTemplatePath = `${templatePath}/RossNanton-Invoice-yyyy.mm.dd.docx`;\nconst wordOutputPath = `${outputPath}/RossNanton-Invoice-${formattedDate}.docx`;\n\ntry {\n  fs.copyFileSync(wordTemplatePath, wordOutputPath);\n  return {\n    json: {\n      success: true,\n      fileCopied: 'Word',\n      sourcePath: wordTemplatePath,\n      outputPath: wordOutputPath\n    }\n  };\n} catch (error) {\n  throw new Error(`Failed to copy Word template: ${error.message}`);\n}"
      },
      "id": "copy_word_template",
      "name": "Copy Word Template",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1000, 200]
    },
    {
      "parameters": {
        "functionCode": "const fs = require('fs');\nconst path = require('path');\n\nconst outputPath = $input.first().json.outputPath;\nconst templatePath = $input.first().json.templatePath;\nconst formattedDate = $input.first().json.formattedDate;\n\nconst excelTemplatePath = `${templatePath}/RossNanton-Timesheet-yyyy.mm.dd.xlsx`;\nconst excelOutputPath = `${outputPath}/RossNanton-Timesheet-${formattedDate}.xlsx`;\n\ntry {\n  fs.copyFileSync(excelTemplatePath, excelOutputPath);\n  return {\n    json: {\n      success: true,\n      fileCopied: 'Excel',\n      sourcePath: excelTemplatePath,\n      outputPath: excelOutputPath,\n      excelFilePath: excelOutputPath\n    }\n  };\n} catch (error) {\n  throw new Error(`Failed to copy Excel template: ${error.message}`);\n}"
      },
      "id": "copy_excel_template",
      "name": "Copy Excel Template",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1000, 400]
    },
    {
      "parameters": {
        "functionCode": "const xlsx = require('xlsx');\nconst fs = require('fs');\n\nconst excelFilePath = $input.last().json.excelFilePath || $input.first().json.excelFilePath;\nconst timesheetDate = $input.first().json.timesheetDate;\n\nif (!excelFilePath) {\n  throw new Error('Excel file path not provided');\n}\n\nif (!timesheetDate) {\n  throw new Error('Timesheet date not provided');\n}\n\ntry {\n  // Read the Excel file\n  const workbook = xlsx.readFile(excelFilePath);\n  const worksheet = workbook.Sheets[workbook.SheetNames[0]];\n  \n  // Update cell E8 with the timesheet date\n  worksheet['E8'] = { v: timesheetDate, t: 's' };\n  \n  // Write the file back\n  xlsx.writeFile(workbook, excelFilePath);\n  \n  return {\n    json: {\n      success: true,\n      fileUpdated: 'Excel (Cell E8)',\n      filePath: excelFilePath,\n      cellE8Updated: timesheetDate\n    }\n  };\n} catch (error) {\n  throw new Error(`Failed to update Excel file: ${error.message}`);\n}"
      },
      "id": "update_excel_cell",
      "name": "Update Excel Cell E8",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1250, 400]
    },
    {
      "parameters": {
        "functionCode": "// Compile final response\nconst inputs = $input.first().json;\n\nreturn {\n  json: {\n    status: 'success',\n    message: 'Timesheet setup completed successfully',\n    details: {\n      timesheetDate: inputs.timesheetDate || $input.all()[0].json.timesheetDate,\n      folderName: inputs.folderName || $input.all()[0].json.folderName,\n      outputPath: inputs.outputPath || $input.all()[0].json.outputPath,\n      filesCreated: [\n        `RossNanton-Invoice-${inputs.formattedDate || $input.all()[0].json.formattedDate}.docx`,\n        `RossNanton-Timesheet-${inputs.formattedDate || $input.all()[0].json.formattedDate}.xlsx (Cell E8 updated)`\n      ]\n    }\n  }\n};"
      },
      "id": "response_node",
      "name": "Response",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1500, 300]
    }
  ],
  "connections": {
    "webhook_trigger": {
      "main": [
        [
          {
            "node": "format_date_node",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "format_date_node": {
      "main": [
        [
          {
            "node": "create_folder_node",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "create_folder_node": {
      "main": [
        [
          {
            "node": "copy_word_template",
            "type": "main",
            "index": 0
          },
          {
            "node": "copy_excel_template",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "copy_word_template": {
      "main": [
        [
          {
            "node": "update_excel_cell",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "copy_excel_template": {
      "main": [
        [
          {
            "node": "update_excel_cell",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "update_excel_cell": {
      "main": [
        [
          {
            "node": "response_node",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "response_node": {
      "main": [
        [
          {
            "node": "webhook_trigger",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
