{
  "name": "Timesheet Setup - Create Dated Folder & Files",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "timesheet-setup",
        "responseMode": "lastNode",
        "options": {}
      },
      "id": "webhook_trigger",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300]
    },
    {
      "parameters": {
        "functionCode": "// Parse incoming timesheet date and format it\nconst timesheetDate = $input.first().json.body?.timesheetDate;\n\nif (!timesheetDate) {\n  throw new Error('timesheetDate is required (format: M/D/YYYY or MM/DD/YYYY)');\n}\n\n// Parse date\nconst parts = timesheetDate.split('/');\nif (parts.length !== 3) {\n  throw new Error('Invalid date format. Use M/D/YYYY');\n}\n\nconst month = parseInt(parts[0]).toString().padStart(2, '0');\nconst day = parseInt(parts[1]).toString().padStart(2, '0');\nconst year = parts[2];\n\nconst formattedDate = `${year}.${month}.${day}`;\nconst folderName = formattedDate;\nconst basePath = '/app/n8n/timesheet/output';\nconst outputPath = `${basePath}/${folderName}`;\nconst templatePath = '/app/n8n/timesheet/n8n-workflows/templates';\n\nreturn {\n  json: {\n    timesheetDate,\n    formattedDate,\n    folderName,\n    outputPath,\n    templatePath,\n    basePath\n  }\n};"
      },
      "id": "format_date_node",
      "name": "Format Date",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [500, 300]
    },
    {
      "parameters": {
        "command": "mkdir -p {{ $json.outputPath }}"
      },
      "id": "bash_create_folder",
      "name": "Create Output Folder",
      "type": "n8n-nodes-base.bash",
      "typeVersion": 1,
      "position": [750, 300]
    },
    {
      "parameters": {
        "command": "cp {{ $json.templatePath }}/RossNanton-Invoice-yyyy.mm.dd.docx {{ $json.outputPath }}/RossNanton-Invoice-{{ $json.formattedDate }}.docx"
      },
      "id": "bash_copy_word",
      "name": "Copy Word Template",
      "type": "n8n-nodes-base.bash",
      "typeVersion": 1,
      "position": [950, 150]
    },
    {
      "parameters": {
        "command": "cp {{ $json.templatePath }}/RossNanton-Timesheet-yyyy.mm.dd.xlsx {{ $json.outputPath }}/RossNanton-Timesheet-{{ $json.formattedDate }}.xlsx"
      },
      "id": "bash_copy_excel",
      "name": "Copy Excel Template",
      "type": "n8n-nodes-base.bash",
      "typeVersion": 1,
      "position": [950, 450]
    },
    {
      "parameters": {
        "functionCode": "const xlsx = require('xlsx');\nconst path = require('path');\n\nconst outputPath = $input.first().json.outputPath;\nconst formattedDate = $input.first().json.formattedDate;\nconst timesheetDate = $input.first().json.timesheetDate;\n\nconst excelFilePath = `${outputPath}/RossNanton-Timesheet-${formattedDate}.xlsx`;\n\nif (!timesheetDate) {\n  throw new Error('Timesheet date not provided');\n}\n\ntry {\n  // Read the Excel file\n  const workbook = xlsx.readFile(excelFilePath);\n  const worksheet = workbook.Sheets[workbook.SheetNames[0]];\n  \n  // Update cell E8 with the timesheet date\n  worksheet['E8'] = { v: timesheetDate, t: 's' };\n  \n  // Write the file back\n  xlsx.writeFile(workbook, excelFilePath);\n  \n  return {\n    json: {\n      success: true,\n      fileUpdated: 'Excel (Cell E8)',\n      filePath: excelFilePath,\n      cellE8Updated: timesheetDate,\n      timesheetDate: timesheetDate,\n      formattedDate: formattedDate,\n      folderName: $input.first().json.folderName,\n      outputPath: outputPath\n    }\n  };\n} catch (error) {\n  throw new Error(`Failed to update Excel file: ${error.message}`);\n}"
      },
      "id": "update_excel_cell",
      "name": "Update Excel Cell E8",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1150, 300]
    },
    {
      "parameters": {
        "functionCode": "// Compile final response\nconst result = $input.first().json;\n\nreturn {\n  json: {\n    status: 'success',\n    message: 'Timesheet setup completed successfully',\n    details: {\n      timesheetDate: result.timesheetDate,\n      folderName: result.folderName,\n      outputPath: result.outputPath,\n      filesCreated: [\n        `RossNanton-Invoice-${result.formattedDate}.docx`,\n        `RossNanton-Timesheet-${result.formattedDate}.xlsx (Cell E8 updated: ${result.timesheetDate})`\n      ]\n    }\n  }\n};"
      },
      "id": "build_response",
      "name": "Build Response",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [1350, 300]
    }
  ],
  "connections": {
    "webhook_trigger": {
      "main": [
        [
          {
            "node": "format_date_node",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "format_date_node": {
      "main": [
        [
          {
            "node": "bash_create_folder",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "bash_create_folder": {
      "main": [
        [
          {
            "node": "bash_copy_word",
            "type": "main",
            "index": 0
          },
          {
            "node": "bash_copy_excel",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "bash_copy_word": {
      "main": [
        [
          {
            "node": "update_excel_cell",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "bash_copy_excel": {
      "main": [
        [
          {
            "node": "update_excel_cell",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "update_excel_cell": {
      "main": [
        [
          {
            "node": "build_response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
